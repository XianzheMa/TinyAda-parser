procedure p5 is


id1, id2, id3 : name;
id1, id2, id3 : name(1,2,3);
id1, id2, id3 : range 3 + 5 .. not foo mod (bar ** 2);
id1, id2, id3 : array(symmetric, diagonal, positive(3,4)) of matrix;
id1 : (enum1, enum2, enum3);


id1 := constant : 3 * 4 and 4 mod 7 = 0;

begin
    exit;
    null;
    func(foo, 2 + 2);
    func(7, bar) := getCode(expr) or 6 * 8;
    while getNumber(matrix) < 5 loop
        null;
    end loop;

    if i < maxInt then
        seq;
        seq2;
    end if;
end p5;
